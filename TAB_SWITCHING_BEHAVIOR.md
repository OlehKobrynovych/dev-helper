# 🔄 Поведінка перемикання табів

## Поточна поведінка:

При перемиканні табів модалка **закривається і відкривається знову**.

### Чому так:

```javascript
perfTab.onclick = function () {
  currentTab = "performance";
  modal.remove(); // Закриваємо модалку
  showModal(); // Відкриваємо знову з новим табом
};
```

**Причини такої реалізації:**

1. **Простота коду** - не потрібно управляти станом
2. **Очищення ресурсів** - всі інтервали/таймери автоматично зупиняються
3. **Свіжий стан** - кожен таб завантажується з нуля

## Альтернативний підхід (складніший):

### Оновлювати тільки контент без закриття:

```javascript
let modalBody = null;
let activeIntervals = [];

perfTab.onclick = function() {
  currentTab = 'performance';

  // Очищаємо старі інтервали
  activeIntervals.forEach(id => clearInterval(id));
  activeIntervals = [];

  // Оновлюємо тільки body
  modalBody.innerHTML = '...';

  // Запускаємо нові інтервали
  const interval = setInterval(...);
  activeIntervals.push(interval);
};
```

**Проблеми:**

- Потрібно відстежувати всі інтервали/таймери
- Потрібно відстежувати всі requestAnimationFrame
- Потрібно відстежувати всі event listeners
- Складніший код
- Більше місць для помилок

## Рекомендація:

**Залишити як є** - закриття і відкриття модалки:

### Переваги:

✅ Простий код  
✅ Автоматичне очищення ресурсів  
✅ Немає memory leaks  
✅ Кожен таб завжди в свіжому стані

### Недоліки:

⚠️ Візуально помітно закриття/відкриття  
⚠️ Втрачається scroll position  
⚠️ Втрачається стан (наприклад, результати аналізу)

## Покращення які можна додати:

### 1. Зберігати стан між перемиканнями:

```javascript
let savedStates = {
  errors: null,
  performance: null,
  code: null,
};

// При закритті зберігаємо
savedStates[currentTab] = {
  scrollPosition: body.scrollTop,
  content: body.innerHTML,
};

// При відкритті відновлюємо
if (savedStates[currentTab]) {
  body.innerHTML = savedStates[currentTab].content;
  body.scrollTop = savedStates[currentTab].scrollPosition;
}
```

### 2. Додати анімацію переходу:

```javascript
modal.style.opacity = "0";
setTimeout(function () {
  modal.remove();
  showModal();
  modal.style.opacity = "1";
}, 200);
```

### 3. Використати CSS transitions:

```css
.modal {
  transition: opacity 0.2s ease;
}
```

## Висновок:

Поточна реалізація **працює коректно**. Закриття і відкриття модалки - це **нормальна поведінка**, яка:

- Гарантує очищення ресурсів
- Запобігає memory leaks
- Спрощує код

Якщо потрібно покращити UX, можна додати:

1. Збереження стану між перемиканнями
2. Плавну анімацію переходу
3. Індикатор завантаження

Але базова функціональність працює правильно! ✅
