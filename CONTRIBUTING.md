# 🤝 Внесок у DevHelper

Дякуємо за інтерес до покращення DevHelper! Ми вітаємо будь-які внески.

## Як допомогти

### Повідомити про баг

1. Перевірте чи немає вже такого [issue](https://github.com/your-username/devhelper/issues)
2. Створіть новий issue з детальним описом:
   - Кроки для відтворення
   - Очікувана поведінка
   - Фактична поведінка
   - Скріншоти (якщо можливо)
   - Версія браузера/Node.js

### Запропонувати нову функцію

1. Створіть issue з тегом "enhancement"
2. Опишіть:
   - Яку проблему вирішує
   - Як це має працювати
   - Приклади використання

### Внести код

1. **Fork репозиторій**

   ```bash
   git clone https://github.com/your-username/devhelper.git
   cd devhelper
   ```

2. **Створіть гілку**

   ```bash
   git checkout -b feature/amazing-feature
   # або
   git checkout -b fix/bug-fix
   ```

3. **Встановіть залежності**

   ```bash
   npm install
   ```

4. **Запустіть dev сервер**

   ```bash
   npm run dev
   ```

5. **Внесіть зміни**
   - Дотримуйтесь стилю коду
   - Додайте коментарі де потрібно
   - Оновіть документацію

6. **Перевірте код**

   ```bash
   npm run lint
   npm run build
   ```

7. **Закомітьте зміни**

   ```bash
   git add .
   git commit -m "feat: додано нову функцію"
   ```

   Формат commit messages:
   - `feat:` - нова функція
   - `fix:` - виправлення бага
   - `docs:` - зміни в документації
   - `style:` - форматування коду
   - `refactor:` - рефакторинг
   - `test:` - додавання тестів
   - `chore:` - інші зміни

8. **Push та створіть Pull Request**
   ```bash
   git push origin feature/amazing-feature
   ```

## Стиль коду

### TypeScript/JavaScript

```typescript
// ✅ Добре
export function captureError(type: ErrorType, message: string): void {
  // Код
}

// ❌ Погано
export function captureError(type, message) {
  // Код
}
```

### React компоненти

```typescript
// ✅ Добре
export function DevHelperWidget({ errors, onClear }: Props) {
  return (
    <div className="widget">
      {/* JSX */}
    </div>
  );
}

// ❌ Погано
export default ({ errors, onClear }) => {
  return <div>{/* JSX */}</div>;
}
```

### Іменування

- Компоненти: `PascalCase` (DevHelperWidget)
- Функції: `camelCase` (captureError)
- Константи: `UPPER_SNAKE_CASE` (MAX_ERRORS)
- Файли: `kebab-case` (devhelper-core.ts)

## Структура проекту

```
devhelper/
├── src/
│   ├── app/              # Next.js app router
│   │   ├── api/          # API routes
│   │   └── page.tsx      # Головна сторінка
│   ├── components/       # React компоненти
│   ├── hooks/            # Custom hooks
│   ├── lib/              # Утиліти та core логіка
│   └── types/            # TypeScript типи
├── public/               # Статичні файли
└── docs/                 # Документація
```

## Тестування

```bash
# Запустити dev сервер
npm run dev

# Білд проекту
npm run build

# Запустити production
npm start

# Лінтинг
npm run lint
```

## Документація

При додаванні нової функції:

1. Оновіть README.md
2. Додайте приклад в EXAMPLES.md
3. Оновіть CHANGELOG.md
4. Додайте JSDoc коментарі в код

```typescript
/**
 * Перехоплює помилки з консолі
 * @param type - Тип помилки (error, warning, info)
 * @param args - Аргументи з console
 * @param extra - Додаткова інформація
 */
private captureError(
  type: 'error' | 'warning' | 'info',
  args: any[],
  extra?: Partial<ConsoleError>
) {
  // Код
}
```

## Що НЕ приймається

- Код без типізації TypeScript
- Зміни без опису в PR
- Код що ламає існуючий функціонал
- Код без коментарів (де потрібно)
- Великі PR без попереднього обговорення

## Питання?

- Створіть [issue](https://github.com/your-username/devhelper/issues)
- Напишіть в [Discussions](https://github.com/your-username/devhelper/discussions)

## Ліцензія

Внесок в проект означає згоду з [MIT ліцензією](LICENSE).

---

Дякуємо за ваш внесок! 🎉
