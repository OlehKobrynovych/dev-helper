# 🔍 Code Analysis - Unused Code Detector

## Що додано:

Новий таб **"🔍 Code Analysis"** з детектором невикористаного коду!

## Функції:

### 1. **🎨 Невикористані CSS класи**

Знаходить CSS селектори, які не використовуються на сторінці:

- Перевіряє всі завантажені stylesheets
- Шукає селектори без відповідних DOM елементів
- Показує розмір кожного невикористаного правила
- Вказує джерело (файл або inline)

**Приклад:**

```css
/* Невикористаний клас */
.old-button { ... }  /* 250 B */
.deprecated-style { ... }  /* 180 B */
```

### 2. **⚡ Невикористані функції**

Виявляє функції, які ніколи не викликаються:

- Аналізує JavaScript код
- Знаходить функції без посилань
- Показує розмір коду функції
- Вказує файл розташування

**Приклад:**

```javascript
// Невикористана функція
function oldLegacyFunction() { ... }  // 250 B
function deprecatedHelper() { ... }   // 180 B
```

### 3. **📦 Невикористані imports**

Знаходить імпорти, які не використовуються в коді:

- Перевіряє всі import statements
- Виявляє невикористані змінні з imports
- Показує розмір імпортованого коду
- Допомагає зменшити bundle size

**Приклад:**

```javascript
// Невикористані imports
import { unused } from "lib"; // 150 B
import oldLib from "old"; // 200 B
```

### 4. **📝 Невикористані змінні**

Виявляє змінні, які оголошені але не використовуються:

- Знаходить const/let/var без посилань
- Показує розташування
- Допомагає очистити код

**Приклад:**

```javascript
// Невикористані змінні
const unusedVar = 123; // 50 B
let oldData = []; // 80 B
```

## Як використовувати:

### Крок 1: Відкрити аналізатор

1. Відкрийте DevHelper віджет (🐛)
2. Натисніть на таб **"🔍 Code Analysis"**

### Крок 2: Запустити аналіз

1. Натисніть кнопку **"🔍 Почати аналіз"**
2. Зачекайте кілька секунд
3. Перегляньте результати

### Крок 3: Переглянути результати

**Загальна статистика:**

```
📊 Результати аналізу

Невикористаний CSS:      15 класів
Невикористані функції:    3 функції
Невикористані imports:    2 імпорти
Невикористані змінні:     2 змінні

💾 Потенційна економія: 12.5 KB
```

**Детальний список:**

- Кожен елемент показує назву, розмір та розташування
- Кольорове кодування по типу
- Можливість скопіювати для видалення

## Приклад результатів:

### Невикористані CSS класи:

```
🎨 Невикористані CSS класи (15)

.old-button              250 B
📄 styles.css

.deprecated-header       180 B
📄 main.css

.unused-modal           320 B
📄 components.css

... та ще 12 класів
```

### Невикористані функції:

```
⚡ Невикористані функції (3)

oldLegacyFunction()     250 B
📄 utils.js

deprecatedHelper()      180 B
📄 helpers.js

unusedValidator()       320 B
📄 validators.js
```

## Рекомендації:

Після аналізу ви отримаєте конкретні поради:

✅ **Для CSS:**

- Видаліть невикористані класи
- Використайте PurgeCSS або Tailwind JIT
- Налаштуйте автоматичне очищення

✅ **Для JavaScript:**

- Видаліть невикористані функції
- Очистіть imports
- Налаштуйте ESLint з "no-unused-vars"
- Увімкніть tree-shaking у bundler

✅ **Для оптимізації:**

- Зменшіть розмір bundle
- Покращіть час завантаження
- Очистіть код для кращої читабельності

## Технічні деталі:

### Як працює аналіз CSS:

```javascript
1. Отримує всі document.styleSheets
2. Перебирає всі CSS правила
3. Для кожного селектора:
   - Виконує document.querySelectorAll(selector)
   - Якщо елементів 0 - селектор невикористаний
4. Підраховує розмір правила
```

### Обмеження:

⚠️ **CSS:**

- Не аналізує динамічно додані класи
- Пропускає складні селектори з псевдо-класами
- CORS може блокувати зовнішні stylesheets

⚠️ **JavaScript:**

- Базовий аналіз (демо-версія)
- Для повного аналізу потрібен статичний аналізатор
- Не бачить динамічні виклики функцій

⚠️ **Загальне:**

- Аналіз тільки завантаженого коду
- Не бачить lazy-loaded модулів
- Потребує додаткових інструментів для production

## Інструменти для глибшого аналізу:

### 1. **webpack-bundle-analyzer**

```bash
npm install --save-dev webpack-bundle-analyzer
```

Візуалізує розмір bundle та залежності

### 2. **ESLint**

```json
{
  "rules": {
    "no-unused-vars": "error",
    "no-unused-imports": "error"
  }
}
```

Автоматично знаходить невикористаний код

### 3. **PurgeCSS**

```bash
npm install --save-dev @fullhuman/postcss-purgecss
```

Автоматично видаляє невикористаний CSS

### 4. **Chrome Coverage**

```
DevTools → More tools → Coverage
```

Показує покриття коду в реальному часі

## Практичні сценарії:

### Сценарій 1: Рефакторинг старого проекту

```
1. Запустити аналіз
2. Знайдено 50 невикористаних CSS класів
3. Видалити їх
4. Економія: 15 KB CSS
5. Швидше завантаження на 200ms
```

### Сценарій 2: Очищення після видалення функції

```
1. Видалили стару функцію
2. Запустили аналіз
3. Знайшли 5 невикористаних imports
4. Видалили їх
5. Bundle зменшився на 50 KB
```

### Сценарій 3: Оптимізація перед production

```
1. Перед деплоєм запустити аналіз
2. Знайти весь dead code
3. Очистити проект
4. Зменшити bundle size
5. Покращити performance
```

## Інтеграція з CI/CD:

Можна автоматизувати перевірку:

```yaml
# .github/workflows/code-analysis.yml
- name: Check unused code
  run: |
    npm run analyze
    npm run lint
    npm run test:coverage
```

## Доступність:

✅ **Next.js компонент** - повна функціональність  
✅ **Integration Script** - працює на будь-якому сайті  
✅ **Однаковий інтерфейс** - в обох версіях

## Переваги:

✅ **Швидкий аналіз** - результати за секунди  
✅ **Візуальне відображення** - легко побачити проблеми  
✅ **Конкретні рекомендації** - що саме видалити  
✅ **Підрахунок економії** - скільки можна зберегти  
✅ **Кольорове кодування** - легко розрізнити типи  
✅ **Інтеграція з DevHelper** - все в одному місці  
✅ **Працює скрізь** - як у Next.js, так і в integration script

## Що далі:

Плануються покращення:

- Глибший статичний аналіз JavaScript
- Аналіз lazy-loaded модулів
- Інтеграція з webpack stats
- Історія аналізів
- Експорт результатів
- Автоматичне видалення (з підтвердженням)
- Аналіз TypeScript типів

---

Тепер ви можете легко знайти та видалити невикористаний код для оптимізації вашого додатку! 🚀
