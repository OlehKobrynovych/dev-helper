# Тестування відображення інформації про файл

## Що було додано:

### 1. **Автоматичне витягування інформації про файл**

- Додано функцію `extractFileInfo()` в `devhelper-core.ts`
- Парсить stack trace для отримання назви файлу, номера рядка та колонки
- Працює як для console.error/warn, так і для runtime помилок

### 2. **Покращене відображення у віджеті**

- Назва файлу відображається з іконкою 📄
- Номер рядка та колонки виділені синім кольором
- Можна натиснути на інформацію про файл, щоб скопіювати розташування

### 3. **Оновлений integration script**

- Жовтий блок з інформацією про файл
- Формат: `📄 Файл: filename.js Рядок: 45 Колонка: 12`
- Автоматично витягує назву файлу з повного шляху

## Як протестувати:

### Варіант 1: У вашому проекті

```javascript
// Відкрийте консоль браузера (F12) та виконайте:
console.error("Тестова помилка");

// Або створіть runtime помилку:
throw new Error("Тестова runtime помилка");
```

### Варіант 2: У тестовій вкладці

1. Відкрийте DevHelper віджет (кнопка 🐛)
2. Перейдіть на вкладку "🧪 Тестування"
3. Натисніть будь-яку кнопку для генерації помилки
4. Поверніться на вкладку "Помилки"
5. Побачите інформацію про файл під кожною помилкою

## Приклад відображення:

```
❌ Тестова помилка від DevHelper

📄 Файл: DevHelperWidget.tsx Рядок: 45 Колонка: 12

🕐 07.11.2025, 14:30:45
```

## Формат даних:

```typescript
interface ConsoleError {
  type: "error" | "warning" | "info";
  message: string;
  stack?: string;
  timestamp: number;
  url?: string; // Повний шлях до файлу
  lineNumber?: number; // Номер рядка
  columnNumber?: number; // Номер колонки
  fileName?: string; // Тільки назва файлу (нове!)
  // ... інші поля
}
```

## Переваги:

✅ Швидко знайти файл з помилкою  
✅ Точний номер рядка та колонки  
✅ Копіювання одним кліком  
✅ Працює автоматично для всіх типів помилок  
✅ Підтримка як у віджеті, так і в integration script
