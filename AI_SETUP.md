# 🤖 Налаштування AI аналізу помилок

DevHelper тепер підтримує AI-аналіз помилок за допомогою OpenAI GPT-4!

---

## ✨ Що дає AI аналіз?

### Без AI (базовий аналіз):

```
❌ TypeError: Cannot read property 'map' of undefined

Рекомендації:
• Використовуйте optional chaining
• Додайте перевірку
• Ініціалізуйте змінні
```

### З AI (розумний аналіз):

```
❌ TypeError: Cannot read property 'map' of undefined

🤖 AI АНАЛІЗ [Висока важливість]
Помилка виникла через спробу викликати метод .map() на undefined
значенні. Це означає, що масив даних не був завантажений або
ініціалізований до моменту рендерингу компонента. Найімовірніше,
це асинхронна проблема - дані ще не прийшли з API.

💡 РЕКОМЕНДАЦІЇ:
• Додайте перевірку стану завантаження: if (loading) return <Loader />
• Використовуйте optional chaining: data?.map() замість data.map()
• Ініціалізуйте стан з порожнім масивом: useState([])
• Додайте fallback: {data?.map() || <EmptyState />}
• Перевірте чи правильно обробляється Promise в useEffect
```

---

## 🚀 Швидке налаштування

### Крок 1: Отримайте OpenAI API ключ

1. Перейдіть на https://platform.openai.com/api-keys
2. Увійдіть або зареєструйтесь
3. Натисніть "Create new secret key"
4. Скопіюйте ключ (він показується тільки один раз!)

### Крок 2: Додайте ключ в проект

#### Локально:

Створіть файл `.env.local`:

```env
OPENAI_API_KEY=sk-proj-ваш-ключ-тут
```

#### На Netlify:

1. Site settings → Environment variables
2. Додайте змінну:
   - **Key:** `OPENAI_API_KEY`
   - **Value:** `sk-proj-ваш-ключ-тут`
   - **Scopes:** Всі
3. Trigger deploy → Deploy site

#### На Vercel:

1. Settings → Environment Variables
2. Додайте:
   - **Name:** `OPENAI_API_KEY`
   - **Value:** `sk-proj-ваш-ключ-тут`
   - **Environment:** Production, Preview, Development
3. Redeploy

### Крок 3: Готово!

Тепер у віджеті з'явиться кнопка **🤖 AI** для аналізу помилок.

---

## 💡 Як використовувати

### 1. Викличте помилку

```javascript
console.error("Test error");
```

### 2. Відкрийте віджет

Натисніть кнопку 🐛 в правому нижньому куті

### 3. Натисніть "🤖 AI"

Кнопка з'явиться поруч з кнопкою "Звіт"

### 4. Дочекайтесь аналізу

Зазвичай займає 2-5 секунд

### 5. Перегляньте результат

Кожна помилка тепер матиме:

- 🤖 **AI АНАЛІЗ** - детальне пояснення
- 💡 **РЕКОМЕНДАЦІЇ** - конкретні кроки для виправлення
- **Рівень важливості** - критична/висока/середня/низька
- **Категорія** - syntax/runtime/network/type/logic

---

## 🎯 Приклади AI аналізу

### Приклад 1: Undefined помилка

**Помилка:**

```
TypeError: Cannot read property 'name' of undefined
```

**AI Аналіз:**

```
Ви намагаєтесь отримати доступ до властивості 'name' об'єкта,
який є undefined. Це часто трапляється коли:
1. Дані ще не завантажились з API
2. Об'єкт не був правильно ініціалізований
3. Умова в коді пропустила перевірку на існування

Рекомендації:
• Використовуйте user?.name замість user.name
• Додайте перевірку: if (user && user.name)
• Перевірте чи дані завантажились: if (loading) return null
• Додайте default значення: const name = user?.name || 'Guest'
```

### Приклад 2: Network помилка

**Помилка:**

```
Failed to fetch: NetworkError when attempting to fetch resource
```

**AI Аналіз:**

```
Мережевий запит не вдався. Можливі причини:
1. Сервер недоступний або повільно відповідає
2. CORS політика блокує запит
3. Неправильний URL або endpoint
4. Відсутнє інтернет з'єднання

Рекомендації:
• Додайте try/catch для обробки помилок
• Перевірте URL: console.log(apiUrl)
• Додайте timeout для запиту
• Перевірте CORS headers на сервері
• Додайте retry логіку з exponential backoff
• Покажіть користувачу зрозуміле повідомлення
```

### Приклад 3: React помилка

**Помилка:**

```
Warning: Each child in a list should have a unique "key" prop
```

**AI Аналіз:**

```
React потребує унікальний key для кожного елемента в списку,
щоб ефективно оновлювати DOM. Без key React не може визначити
які елементи змінились, додались або видалились.

Рекомендації:
• Використовуйте унікальний ID: key={item.id}
• Якщо немає ID, використовуйте комбінацію: key={`${item.name}-${index}`}
• НЕ використовуйте тільки index як key якщо список може змінюватись
• Для статичних списків index підходить: key={index}
• Переконайтесь що key унікальний в межах списку
```

---

## 💰 Вартість

OpenAI GPT-4o-mini (модель яку ми використовуємо):

- **Ціна:** ~$0.15 за 1 мільйон токенів
- **Один аналіз:** ~500 токенів = $0.000075 (менше 0.01 центу)
- **1000 аналізів:** ~$0.075 (7.5 центів)

**Висновок:** Дуже дешево! Навіть 10,000 помилок коштуватиме менше $1.

---

## 🔒 Безпека

### Що відправляється в OpenAI?

Тільки технічна інформація:

- Текст помилки
- Stack trace (перші 500 символів)
- URL сторінки
- Номер рядка

### Що НЕ відправляється?

- Персональні дані користувачів
- Токени або паролі
- Повний код вашого додатку
- API ключі

### Рекомендації:

1. Використовуйте окремий OpenAI ключ для DevHelper
2. Встановіть ліміти витрат в OpenAI Dashboard
3. Не використовуйте AI аналіз для помилок з чутливими даними

---

## 🎛️ Налаштування

### Вимкнути AI аналіз

Просто не додавайте `OPENAI_API_KEY` - DevHelper буде використовувати базовий аналіз.

### Змінити модель

Відредагуйте `src/app/api/devhelper/analyze/route.ts`:

```typescript
model: 'gpt-4o-mini',  // Швидка і дешева
// або
model: 'gpt-4o',       // Більш розумна, але дорожча
```

### Змінити мову

За замовчуванням - українська. Для зміни відредагуйте prompt в `analyze/route.ts`:

```typescript
content: "Ти експерт з JavaScript/TypeScript, який аналізує помилки та надає практичні поради українською мовою.";
// Змініть на:
content: "You are a JavaScript/TypeScript expert who analyzes errors and provides practical advice in English.";
```

---

## 🧪 Тестування

### Локально:

```bash
# Додайте ключ
echo "OPENAI_API_KEY=sk-proj-ваш-ключ" >> .env.local

# Запустіть
npm run dev

# Відкрийте http://localhost:3000
# Викличте помилку
# Натисніть 🤖 AI
```

### На production:

1. Додайте `OPENAI_API_KEY` на Netlify/Vercel
2. Зробіть редеплой
3. Відкрийте ваш сайт
4. Викличте помилку
5. Натисніть 🤖 AI

---

## ❓ FAQ

### Q: Чи обов'язковий OpenAI ключ?

**A:** Ні! Без ключа DevHelper використовує базовий аналіз з готовими рекомендаціями.

### Q: Скільки це коштує?

**A:** Дуже мало - близько $0.075 за 1000 аналізів.

### Q: Чи безпечно?

**A:** Так! Відправляється тільки технічна інформація про помилки.

### Q: Яка модель використовується?

**A:** GPT-4o-mini - швидка, дешева і розумна.

### Q: Чи можна використовувати іншу AI?

**A:** Так! Відредагуйте `analyze/route.ts` для інтеграції з іншими API (Claude, Gemini, тощо).

### Q: Скільки часу займає аналіз?

**A:** Зазвичай 2-5 секунд для всіх помилок.

### Q: Чи працює офлайн?

**A:** Ні, потрібне інтернет з'єднання для OpenAI API.

---

## 🎉 Готово!

Тепер ваш DevHelper має AI-суперсилу! 🤖✨

**Наступні кроки:**

1. ✅ Отримайте OpenAI ключ
2. ✅ Додайте в .env.local або Netlify
3. ✅ Викличте помилку
4. ✅ Натисніть 🤖 AI
5. ✅ Насолоджуйтесь розумними порадами!

---

**Питання? Створіть issue на GitHub!**
