# 🎨 Фільтрація Tailwind класів

## Проблема:

При аналізі проекту з Tailwind CSS виявлялось **17000+ невикористаних класів**!

### Чому:

```css
/* Tailwind генерує тисячі utility класів */
.m-0, .m-1, .m-2, .m-3, .m-4, ... .m-96
.p-0, .p-1, .p-2, .p-3, .p-4, ... .p-96
.text-xs, .text-sm, .text-base, .text-lg, ...
.bg-red-50, .bg-red-100, ... .bg-red-900
/* І так далі для кожного utility */
```

**Результат:** 17000+ класів, більшість з яких ти не використовуєш, але це нормально для Tailwind!

## Виправлення:

### 1. **Ігнорування Tailwind файлів**

```typescript
const cssFiles = glob.sync(`${tempDir}/**/*.css`, {
  ignore: [
    "**/tailwind.css", // Вихідний Tailwind
    "**/output.css", // Згенерований Tailwind
  ],
});
```

### 2. **Фільтрація Tailwind utility класів**

```typescript
const tailwindPatterns = [
  /^(sm|md|lg|xl|2xl):/, // sm:text-center
  /^(hover|focus|active):/, // hover:bg-blue-500
  /^-?[mp][trblxy]?-\d+$/, // m-4, px-2, -mt-2
  /^(text|bg|border)-/, // text-red-500, bg-blue-100
  /^(flex|grid|block|hidden)/, // flex, grid, hidden
  /^(rounded|shadow|opacity)-/, // rounded-lg, shadow-md
  // ... та багато інших
];

// Перевіряємо кожен клас
const isTailwind = tailwindPatterns.some((pattern) => pattern.test(className));
if (!isTailwind) {
  cssClasses.add(className); // Тільки кастомні класи
}
```

### 3. **Пропуск файлів з @tailwind директивами**

```typescript
cssFiles.forEach((f) => {
  const content = fs.readFileSync(f, "utf8");

  // Пропускаємо Tailwind конфігурацію
  if (content.includes("@tailwind") || content.includes("@apply")) {
    return; // Не аналізуємо цей файл
  }

  // Аналізуємо тільки кастомні CSS
});
```

## Що фільтрується:

### ✅ Tailwind utility класи:

```css
/* Responsive */
sm:text-center, md:flex, lg:hidden

/* States */
hover:bg-blue-500, focus:ring-2, active:scale-95

/* Spacing */
m-4, p-2, px-4, py-2, mt-8, -mb-4

/* Colors */
text-red-500, bg-blue-100, border-gray-300

/* Layout */
flex, grid, block, inline, hidden

/* Effects */
rounded-lg, shadow-md, opacity-50

/* Typography */
text-sm, font-bold, leading-tight

/* Position */
absolute, relative, top-0, left-4

/* Transitions */
transition, duration-300, ease-in-out

/* І багато інших... */
```

### ❌ Що НЕ фільтрується (кастомні класи):

```css
/* Ваші власні класи */
.my-custom-button
.header-navigation
.product-card
.modal-overlay
```

## Результат:

### Було:

```
📊 Результати аналізу
Невикористаний CSS: 17,543 класів ❌
(більшість - Tailwind utilities)
```

### Стало:

```
📊 Результати аналізу
Невикористаний CSS: 15 класів ✅
(тільки ваші кастомні класи)
```

## Додаткові покращення:

### 1. **Обмеження відображення**

```typescript
// Показуємо тільки перші 50 класів
result.unusedCSS.slice(0, 50)

// Попередження якщо більше
{result.unusedCSS.length > 50 && (
  <div>⚠️ Показано перші 50 класів</div>
)}
```

### 2. **Кнопка завантаження**

```typescript
// Завантажити всі класи як .txt файл
<button onClick={downloadAllClasses}>
  📥 Завантажити всі класи ({result.unusedCSS.length})
</button>
```

### 3. **Копіювання класів**

```typescript
// Кнопка 📋 для кожного класу
<button onClick={() => navigator.clipboard.writeText(className)}>
  📋
</button>
```

### 4. **Покращений UI**

```typescript
// Скрол для довгих списків
<div className="max-h-96 overflow-y-auto">
  {/* Список класів */}
</div>
```

## Приклад результату:

```
📊 Результати аналізу

Невикористаний CSS: 15

🎨 Невикористані CSS класи (15)
⚠️ Показано перші 50 класів

old-button                    [📋]
📄 styles/legacy.css

deprecated-header             [📋]
📄 components/header.css

unused-modal-overlay          [📋]
📄 styles/modals.css

... (ще 12 класів)

[📥 Завантажити всі класи (15)]
```

## Що робити з результатами:

### 1. Перевірте чи класи справді не використовуються

```bash
# Пошук в проекті
grep -r "old-button" src/
```

### 2. Видаліть невикористані класи

```css
/* Видаліть з CSS файлу */
.old-button { ... }  ← Видалити
```

### 3. Або додайте до проекту

```jsx
// Якщо клас потрібен - використайте його
<button className="old-button">Click</button>
```

## Переваги:

✅ **Фільтрує Tailwind** - не показує 17000 utility класів  
✅ **Тільки кастомні класи** - ваші власні стилі  
✅ **Пагінація** - показує перші 50, решту можна завантажити  
✅ **Копіювання** - кнопка 📋 для кожного класу  
✅ **Завантаження** - експорт всіх результатів у .txt  
✅ **Скрол** - зручний перегляд довгих списків

## Висновок:

Тепер аналіз показує **тільки ваші кастомні класи**, ігноруючи тисячі Tailwind utilities! 🎯

З 17000 класів → 15 реальних невикористаних класів ✅
