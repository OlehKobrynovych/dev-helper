# 🚀 Сканування проекту через DevHelper Script

## ✅ Що додано:

Тепер DevHelper Script може сканувати весь проект (не тільки поточну сторінку)!

### Нова функція в скрипті:

- **🚀 Сканувати проект** - повний аналіз всіх файлів проекту на сервері
- **🔍 Сканувати сторінку** - швидкий аналіз CSS на поточній сторінці

## 🎯 Як це працює:

### 1. Підключення скрипта до проекту:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>My Project</title>
  </head>
  <body>
    <!-- Ваш контент -->

    <!-- DevHelper Script -->
    <script src="https://your-domain.com/api/devhelper/script"></script>
    <script>
      DevHelper.init({
        apiKey: "your-api-key",
        projectId: "my-project",
        devMode: true, // Показує віджет
      });
    </script>
  </body>
</html>
```

### 2. Використання:

1. Відкрийте проект в браузері
2. Натисніть кнопку 🐛 внизу справа
3. Перейдіть на вкладку "🔍 Code Analysis"
4. Натисніть "🚀 Сканувати проект"
5. Чекайте результати (до 30 секунд)

## 📊 Що показує:

### Статистика:

```
📊 Результати аналізу проекту
┌─────────────────────────────┐
│ CSS файлів: 15              │
│ JS файлів: 42               │
│ Всього CSS класів: 234      │
│ Всього функцій: 89          │
└─────────────────────────────┘

Невикористаний CSS: 12 (5.1% від всіх)
Невикористані функції: 3 (3.4% від всіх)
```

### Невикористані CSS класи:

```
🎨 Невикористані CSS класи (12)
⚠️ Показано перші 50 класів з 12

old-button
📄 /src/styles/legacy.css

deprecated-header
📄 /src/components/header.css
```

### Невикористані функції:

```
⚡ Невикористані функції (3)

oldLegacyFunction()
deprecatedHelper()
unusedValidator()
```

## 🔧 Технічні деталі:

### API запит:

```javascript
fetch("https://your-domain.com/api/devhelper/analyze-project", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "X-API-Key": "your-api-key",
  },
  body: JSON.stringify({
    scanCurrentProject: true,
  }),
});
```

### Відповідь:

```json
{
  "success": true,
  "unusedCSS": [{ "name": "old-button", "location": "/src/styles/legacy.css" }],
  "unusedFunctions": ["oldFunction"],
  "stats": {
    "totalCSSClasses": 234,
    "totalFunctions": 89,
    "unusedCSSCount": 12,
    "unusedFunctionsCount": 3,
    "cssFilesAnalyzed": 15,
    "jsFilesAnalyzed": 42
  }
}
```

## 🎨 Різниця між кнопками:

### 🚀 Сканувати проект:

- ✅ Сканує всі файли проекту на сервері
- ✅ Повний аналіз CSS та JavaScript
- ✅ Показує файли де знаходяться класи
- ✅ Фільтрує Tailwind utility класи
- ⏱️ Займає до 30 секунд
- 📊 Детальна статистика

### 🔍 Сканувати сторінку:

- ✅ Швидкий аналіз (2-3 секунди)
- ✅ Сканує тільки CSS на поточній сторінці
- ⚠️ Може показати класи які використовуються на інших сторінках
- ⚠️ Не аналізує JavaScript
- 📊 Базова статистика

## 💡 Коли використовувати:

### Сканувати проект:

- Перед релізом для очищення коду
- Для повного аудиту проекту
- Коли потрібна детальна інформація
- Для знаходження невикористаних функцій

### Сканувати сторінку:

- Швидка перевірка під час розробки
- Для перевірки конкретної сторінки
- Коли потрібен швидкий результат
- Для дебагу CSS проблем

## ⚠️ Обмеження:

1. **Динамічні класи** можуть бути позначені як невикористані:

   ```js
   const className = `btn-${type}`; // Може не розпізнатись
   ```

2. **Функції викликані через рефлексію**:

   ```js
   window[functionName](); // Може не розпізнатись
   ```

3. **Класи з інших проектів** (якщо використовується shared CSS)

## 🔒 Безпека:

- Сканування відбувається на вашому сервері
- Не передає код третім особам
- Потребує API ключ для доступу
- Працює тільки в dev mode

## 📦 Що потрібно:

1. DevHelper API запущений на вашому сервері
2. API ключ налаштований
3. Проект має доступ до файлової системи
4. Node.js з необхідними пакетами (glob, typescript, adm-zip)

## 🚀 Приклад використання:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>E-commerce Project</title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    <div class="container">
      <h1 class="title">My Shop</h1>
      <!-- Контент -->
    </div>

    <!-- DevHelper -->
    <script src="https://devhelper.example.com/api/devhelper/script"></script>
    <script>
      DevHelper.init({
        apiKey: "sk_live_abc123",
        projectId: "ecommerce-frontend",
        devMode: true,
      });
    </script>
  </body>
</html>
```

Тепер відкрийте проект, натисніть 🐛 → "🔍 Code Analysis" → "🚀 Сканувати проект" і отримайте повний звіт про невикористаний код! 🎉
