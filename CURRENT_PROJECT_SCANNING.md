# 🔍 Сканування поточного проекту

## ✅ Що змінилось:

### 1. **Сканування без ZIP файлу**

Тепер можна сканувати поточний проект напряму, без створення ZIP архіву!

**Було:**

- Створити ZIP архів проекту
- Завантажити через форму
- Чекати розпакування

**Стало:**

- Натиснути кнопку "🚀 Почати сканування"
- Сканування відбувається напряму з файлової системи
- Швидше та зручніше!

### 2. **Покращена фільтрація Tailwind**

Розширено список Tailwind patterns для фільтрації:

- ✅ Всі кольори: `text-blue-500`, `bg-red-100`
- ✅ Всі розміри: `w-full`, `h-screen`, `min-w-0`
- ✅ Всі стани: `hover:`, `focus:`, `active:`, `visited:`
- ✅ Всі responsive: `sm:`, `md:`, `lg:`, `xl:`, `2xl:`
- ✅ Layout: `flex`, `grid`, `block`, `inline-flex`
- ✅ Position: `absolute`, `relative`, `fixed`, `sticky`
- ✅ Border: `border-2`, `border-t-4`, `rounded-lg`
- ✅ Typography: `font-bold`, `text-lg`, `leading-tight`
- ✅ Spacing: `gap-4`, `space-x-2`, `m-4`, `p-2`
- ✅ Effects: `opacity-50`, `blur-sm`, `shadow-lg`

### 3. **Інформація про файли**

Кожен невикористаний клас тепер показує:

- 📄 Назву файлу де він знаходиться
- 📋 Кнопку копіювання
- 📍 Відносний шлях до файлу

### 4. **Детальна статистика**

```
📊 Результати аналізу
┌─────────────────────────────┐
│ CSS файлів: 15              │
│ JS файлів: 42               │
│ Всього CSS класів: 234      │
│ Всього функцій: 89          │
└─────────────────────────────┘

Невикористаний CSS: 12 (5.1% від всіх)
Невикористані функції: 3 (3.4% від всіх)
```

### 5. **UI покращення**

- Показує перші 50 CSS класів
- Кнопка "Показати всі" для розгортання
- Кнопка "Згорнути" для повернення
- Кнопка "💾 Завантажити TXT" для експорту
- Аналогічно для функцій (перші 30)

## 🚀 Як використовувати:

### Варіант 1: Сканування поточного проекту (РЕКОМЕНДОВАНО)

1. Відкрийте DevHelper Widget
2. Перейдіть на вкладку "Unused Code"
3. Натисніть "🚀 Почати сканування"
4. Чекайте результати (до 30 секунд)

### Варіант 2: Завантаження ZIP (для інших проектів)

1. Створіть ZIP архів проекту
2. Завантажте через форму
3. Чекайте результати

## 📝 Що сканується:

### CSS файли:

- ✅ Всі `.css` файли в проекті
- ❌ Ігнорує: `node_modules/`, `.next/`, `dist/`
- ❌ Ігнорує: файли з `@tailwind` директивами
- ❌ Ігнорує: `tailwind.css`, `output.css`

### JavaScript файли:

- ✅ Всі `.js`, `.jsx`, `.ts`, `.tsx` файли
- ✅ Перевіряє використання CSS класів
- ✅ Аналізує експортовані функції
- ❌ Ігнорує: `node_modules/`, `.next/`, `dist/`

## 🎯 Результат:

### Невикористані CSS класи:

```
old-button                    [📋]
📄 /src/styles/legacy.css

deprecated-header             [📋]
📄 /src/components/header.css
```

### Невикористані функції:

```
oldLegacyFunction()           [📋]
deprecatedHelper()            [📋]
```

## ⚠️ Обмеження:

1. **Динамічні класи** можуть бути позначені як невикористані:

   ```js
   // Може не розпізнатись
   const className = `btn-${type}`;
   ```

2. **Класи з інших сторінок** (якщо сканується тільки одна сторінка)

3. **Функції викликані через рефлексію** можуть бути позначені як невикористані

## 💡 Рекомендації:

1. Використовуйте сканування поточного проекту для швидкого аналізу
2. Перевіряйте результати вручну перед видаленням коду
3. Для production використовуйте:
   - **PurgeCSS** - автоматичне очищення CSS
   - **ESLint** - статичний аналіз JavaScript
   - **webpack-bundle-analyzer** - аналіз bundle
   - **Chrome Coverage** - runtime аналіз

## 🔧 API:

### POST `/api/devhelper/analyze-project`

**Сканування поточного проекту:**

```json
{
  "scanCurrentProject": true
}
```

**Відповідь:**

```json
{
  "success": true,
  "unusedCSS": [{ "name": "old-button", "location": "/src/styles/legacy.css" }],
  "unusedFunctions": ["oldFunction"],
  "stats": {
    "totalCSSClasses": 234,
    "totalFunctions": 89,
    "unusedCSSCount": 12,
    "unusedFunctionsCount": 3,
    "cssFilesAnalyzed": 15,
    "jsFilesAnalyzed": 42
  }
}
```
