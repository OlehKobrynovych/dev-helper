# 🔒 Політика безпеки

## Підтримувані версії

Ми надаємо оновлення безпеки для наступних версій:

| Версія | Підтримка          |
| ------ | ------------------ |
| 1.0.x  | :white_check_mark: |
| < 1.0  | :x:                |

## Повідомлення про вразливості

Якщо ви знайшли вразливість в DevHelper, будь ласка, повідомте нас якомога швидше.

### Як повідомити

**НЕ створюйте публічний issue для вразливостей безпеки.**

Замість цього:

1. Надішліть email на: security@devhelper.com (або створіть приватний Security Advisory на GitHub)
2. Опишіть вразливість детально
3. Додайте кроки для відтворення
4. Вкажіть потенційний вплив

### Що включити в звіт

- Тип вразливості
- Повний шлях до вразливого коду
- Місце вразливого коду (tag/branch/commit або direct URL)
- Спеціальна конфігурація для відтворення
- Покрокова інструкція для відтворення
- Proof-of-concept або exploit код (якщо можливо)
- Вплив вразливості, включаючи як зловмисник може її використати

### Що очікувати

- Підтвердження отримання протягом 48 годин
- Оцінка вразливості протягом 7 днів
- Виправлення критичних вразливостей протягом 30 днів
- Публічне розкриття після виправлення

## Політика розкриття

Ми дотримуємось принципу відповідального розкриття:

1. Вразливість повідомляється приватно
2. Ми підтверджуємо та оцінюємо вразливість
3. Ми розробляємо та тестуємо виправлення
4. Ми випускаємо патч
5. Ми публічно розкриваємо вразливість

## Безпека в DevHelper

### Що ми робимо

- ✅ Регулярні security audits
- ✅ Dependency updates
- ✅ Code reviews
- ✅ Automated security scanning
- ✅ HTTPS для всіх з'єднань

### Що ми НЕ збираємо

- ❌ Персональні дані користувачів
- ❌ Паролі або токени
- ❌ Фінансову інформацію
- ❌ Геолокацію (поки що)
- ❌ Cookies або tracking

### Що ми збираємо

- ✅ Технічна інформація про помилки
- ✅ Stack traces
- ✅ URL сторінки
- ✅ User agent
- ✅ Timestamp

### Зберігання даних

- Помилки зберігаються в пам'яті браузера
- Звіти відправляються через HTTPS
- API ключі передаються в headers
- Немає постійного збереження без згоди

## Best Practices для користувачів

### API ключі

```javascript
// ❌ Погано - ключ в коді
window.DevHelper.init({
  apiKey: "abc123xyz",
  projectId: "my-project",
});

// ✅ Добре - ключ в змінних оточення
window.DevHelper.init({
  apiKey: process.env.DEVHELPER_API_KEY,
  projectId: process.env.DEVHELPER_PROJECT_ID,
});
```

### Production налаштування

```javascript
// ✅ Рекомендовано для production
window.DevHelper.init({
  apiKey: process.env.DEVHELPER_API_KEY,
  projectId: "my-project-prod",
  devMode: false, // Приховати віджет
  autoReport: true, // Автоматичні звіти
});
```

### Фільтрація чутливих даних

```javascript
// Якщо помилка містить чутливі дані
const devHelper = window.DevHelper.init({...});

// Фільтруйте перед відправкою
const errors = devHelper.getErrors().map(error => ({
  ...error,
  message: error.message.replace(/password=\w+/g, 'password=***'),
  stack: error.stack?.replace(/token=\w+/g, 'token=***')
}));
```

## Відомі обмеження

### Поточна версія (1.0.0)

- Немає шифрування даних в transit (використовується HTTPS)
- Немає rate limiting на API
- Немає автентифікації для API endpoints
- Помилки зберігаються в plain text

### Заплановані покращення (1.1.0+)

- [ ] End-to-end encryption
- [ ] Rate limiting
- [ ] API authentication
- [ ] Data encryption at rest
- [ ] GDPR compliance tools

## Compliance

### GDPR

DevHelper розроблений з урахуванням GDPR:

- Мінімальний збір даних
- Прозорість в зборі даних
- Право на видалення даних
- Право на експорт даних

### Рекомендації

Якщо ви використовуєте DevHelper в ЄС:

1. Додайте інформацію про DevHelper в Privacy Policy
2. Отримайте згоду користувачів (якщо потрібно)
3. Надайте можливість opt-out
4. Регулярно видаляйте старі дані

## Security Checklist

Перед деплоєм в production:

- [ ] API ключі в змінних оточення
- [ ] devMode: false для production
- [ ] HTTPS для всіх endpoints
- [ ] Rate limiting налаштовано
- [ ] Логування налаштовано
- [ ] Моніторинг налаштовано
- [ ] Backup налаштовано
- [ ] Privacy Policy оновлено

## Контакти

- Security issues: security@devhelper.com
- General questions: support@devhelper.com
- GitHub Security Advisories: [Create Advisory](https://github.com/your-username/devhelper/security/advisories/new)

## Подяки

Ми дякуємо всім, хто відповідально повідомляє про вразливості. Ваші імена будуть додані в Hall of Fame (з вашого дозволу).

---

**Безпека - наш пріоритет. Дякуємо за допомогу в захисті DevHelper!**

Останнє оновлення: 06.11.2025
