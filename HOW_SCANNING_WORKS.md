# 🔍 Як працює сканування проекту

## Покрокове пояснення:

### Крок 1: Натискання кнопки "🔍 Почати аналіз"

```javascript
analyzeBtn.onclick = function () {
  if (analyzeBtn.disabled) return; // Перевірка чи не запущено вже
  analyzeBtn.disabled = true; // Блокуємо кнопку
  analyzeBtn.textContent = "🔄 Аналіз..."; // Змінюємо текст

  setTimeout(function () {
    // Аналіз відбувається тут
  }, 2000); // Затримка 2 секунди
};
```

**Що відбувається:**

1. Перевіряємо чи кнопка не заблокована
2. Блокуємо кнопку (щоб не натиснули двічі)
3. Змінюємо текст на "🔄 Аналіз..."
4. Запускаємо аналіз через 2 секунди (щоб UI не завис)

---

### Крок 2: Отримання всіх CSS файлів

```javascript
const allStyleSheets = Array.from(document.styleSheets);
```

**Що це робить:**

- `document.styleSheets` - це колекція ВСІХ CSS на сторінці
- Включає:
  - `<link rel="stylesheet" href="styles.css">` - зовнішні файли
  - `<style>...</style>` - inline стилі
  - Динамічно додані стилі через JavaScript

**Приклад:**

```html
<!-- Ваш проект -->
<link rel="stylesheet" href="main.css" />
<link rel="stylesheet" href="components.css" />
<style>
  .custom {
    color: red;
  }
</style>

<!-- Скрипт бачить ВСІ ці файли -->
```

---

### Крок 3: Перебір кожного CSS файлу

```javascript
allStyleSheets.forEach(function (sheet) {
  try {
    const rules = Array.from(sheet.cssRules || []);
    // ...
  } catch (e) {
    // CORS помилка - пропускаємо
  }
});
```

**Що відбувається:**

- Для кожного stylesheet отримуємо всі CSS правила
- `sheet.cssRules` - це всі правила в файлі (селектори + стилі)
- `try/catch` - захист від CORS помилок (зовнішні домени)

**Приклад:**

```css
/* main.css */
.button { ... }      /* ← rule 1 */
.header { ... }      /* ← rule 2 */
.old-class { ... }   /* ← rule 3 */
```

---

### Крок 4: Перевірка кожного селектора

```javascript
rules.forEach(function (rule) {
  if (rule.selectorText) {
    const selector = rule.selectorText;

    // Пропускаємо складні селектори
    if (selector.includes(":") || selector.includes("[")) return;

    try {
      // Шукаємо елементи з цим селектором
      const elements = document.querySelectorAll(selector);

      if (elements.length === 0) {
        // Селектор невикористаний!
        unusedCSS.push({
          name: selector,
          location: sheet.href || "inline",
          size: rule.cssText.length,
        });
      }
    } catch (e) {
      // Помилка парсингу - пропускаємо
    }
  }
});
```

**Детальне пояснення:**

#### 4.1. Отримання селектора

```javascript
const selector = rule.selectorText; // ".button", ".header", тощо
```

#### 4.2. Фільтрація складних селекторів

```javascript
if (selector.includes(":") || selector.includes("[")) return;
```

**Чому пропускаємо:**

- `:hover`, `:active`, `:focus` - псевдо-класи (не можна перевірити)
- `[data-id]`, `[type="text"]` - атрибутні селектори (складні)
- `.button:hover` - комбінації

**Приклад:**

```css
.button { ... }           /* ✅ Перевіряємо */
.button:hover { ... }     /* ❌ Пропускаємо */
[data-id] { ... }         /* ❌ Пропускаємо */
```

#### 4.3. Пошук елементів на сторінці

```javascript
const elements = document.querySelectorAll(selector);
```

**Що це робить:**

- Шукає ВСІ елементи на поточній сторінці з цим селектором
- Повертає NodeList (масив елементів)

**Приклад:**

```html
<!-- HTML на сторінці -->
<div class="button">Click</div>
<div class="header">Title</div>
<!-- Немає елементів з class="old-class" -->
```

```javascript
document.querySelectorAll(".button"); // → [<div>] (1 елемент)
document.querySelectorAll(".header"); // → [<div>] (1 елемент)
document.querySelectorAll(".old-class"); // → [] (0 елементів) ← НЕВИКОРИСТАНИЙ!
```

#### 4.4. Збереження невикористаних

```javascript
if (elements.length === 0) {
  unusedCSS.push({
    name: selector, // ".old-class"
    location: sheet.href || "inline", // "main.css" або "inline"
    size: rule.cssText.length, // 45 (байтів)
  });
}
```

---

### Крок 5: Підрахунок економії

```javascript
const totalWaste = unusedCSS.reduce(function (sum, item) {
  return sum + item.size;
}, 0);
```

**Що це робить:**

- Сумує розміри всіх невикористаних правил
- Результат в байтах

**Приклад:**

```javascript
unusedCSS = [
  { name: '.old-class', size: 45 },
  { name: '.deprecated', size: 60 },
  { name: '.unused', size: 30 }
];

totalWaste = 45 + 60 + 30 = 135 байтів = 0.13 KB
```

---

### Крок 6: Відображення результатів

```javascript
html +=
  "<p>💾 Потенційна економія: " + (totalWaste / 1024).toFixed(1) + " KB</p>";

unusedCSS.slice(0, 10).forEach(function (item) {
  html += "<code>" + item.name + "</code>";
  html += "<span>" + item.size + " B</span>";
  html += "<p>📄 " + item.location + "</p>";
});
```

**Результат:**

```
📊 Результати аналізу

Невикористаний CSS: 15 класів
💾 Потенційна економія: 2.3 KB

🎨 Невикористані CSS класи:
.old-button          250 B
📄 main.css

.deprecated-header   180 B
📄 components.css
```

---

## Візуальна схема роботи:

```
Натискання кнопки
    ↓
Блокування UI
    ↓
document.styleSheets → [main.css, components.css, inline]
    ↓
Для кожного файлу:
    ↓
    sheet.cssRules → [.button, .header, .old-class, ...]
    ↓
    Для кожного правила:
        ↓
        Отримати селектор → ".old-class"
        ↓
        Пропустити складні? (: або [) → Ні
        ↓
        document.querySelectorAll(".old-class") → []
        ↓
        Елементів 0? → Так
        ↓
        Додати до unusedCSS ✅
    ↓
Підрахувати економію
    ↓
Відобразити результати
    ↓
Розблокувати UI
```

---

## Що сканується:

### ✅ Сканується:

- Всі `<link rel="stylesheet">` файли
- Всі `<style>` теги
- Динамічно додані стилі
- Inline стилі
- CSS з того ж домену

### ❌ НЕ сканується:

- CSS з інших доменів (CORS)
- Псевдо-класи (`:hover`, `:focus`)
- Атрибутні селектори (`[data-id]`)
- Класи на інших сторінках (тільки поточна)
- Динамічно додані класи через JS

---

## Обмеження:

### 1. **Тільки поточна сторінка**

```javascript
// Якщо клас використовується на іншій сторінці:
// page1.html → <div class="button">
// page2.html → немає .button
// Аналіз на page2.html покаже .button як невикористаний ❌
```

### 2. **CORS блокування**

```javascript
// Зовнішні CSS з інших доменів:
<link rel="stylesheet" href="https://cdn.example.com/styles.css">
// ❌ Не можна прочитати через CORS
```

### 3. **Динамічні класи**

```javascript
// JavaScript додає клас динамічно:
element.classList.add("dynamic-class");
// ❌ Аналіз не бачить цього
```

---

## Перевірка коду на помилки:

### ✅ Код правильний, але можна покращити:

#### Проблема 1: Пропускаємо багато селекторів

```javascript
// Зараз
if (selector.includes(":") || selector.includes("[")) return;

// Краще
if (selector.match(/:[^:]/)) return; // Тільки псевдо-класи
// Дозволити [class], [id] тощо
```

#### Проблема 2: Не враховуємо медіа-запити

```css
@media (max-width: 768px) {
  .mobile-only { ... }
}
```

Цей клас може бути невикористаний на desktop, але використовуватись на mobile.

#### Проблема 3: Не враховуємо комбінатори

```css
.parent .child { ... }
```

Якщо немає `.parent`, то `.child` теж не використовується.

---

## Висновок:

**Сканування працює так:**

1. ✅ Отримує ВСІ CSS файли з проекту
2. ✅ Перебирає кожне правило
3. ✅ Шукає елементи на сторінці
4. ✅ Якщо елементів 0 - клас невикористаний
5. ✅ Підраховує економію
6. ✅ Відображає результати

**Обмеження:**

- ⚠️ Тільки поточна сторінка
- ⚠️ CORS блокує зовнішні файли
- ⚠️ Не бачить динамічні класи
- ⚠️ Пропускає складні селектори

**Для production використовуйте:**

- PurgeCSS - аналізує весь проект
- UnCSS - видаляє невикористаний CSS
- Chrome Coverage - runtime аналіз
