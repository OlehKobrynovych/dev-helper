# 🧪 Вкладка "Тестування" в DevHelper Script

## ✅ Що додано:

Нова вкладка **"🧪 Тестування"** для швидкого тестування системи відловлювання помилок!

## 🎯 Призначення:

Вкладка дозволяє швидко згенерувати різні типи помилок для тестування:

- Перевірки роботи DevHelper
- Тестування AI аналізу помилок
- Демонстрації можливостей системи
- Навчання команди роботі з помилками

## 📋 Доступні тести:

### 1. ❌ Console Error

Генерує просту помилку в консолі:

```javascript
console.error("Тестова помилка від DevHelper");
```

### 2. ⚠️ Console Warning

Генерує попередження в консолі:

```javascript
console.warn("Тестове попередження від DevHelper");
```

### 3. ⚡ Runtime Error

Генерує runtime помилку з try-catch:

```javascript
try {
  throw new Error("Тестова runtime помилка");
} catch (e) {
  console.error(e);
}
```

### 4. ℹ️ Promise Rejection

Генерує відхилення Promise:

```javascript
Promise.reject("Тестове відхилення Promise").catch(() => {});
```

### 5. ❌ Undefined Error

Генерує помилку доступу до undefined:

```javascript
try {
  const obj = undefined;
  console.log(obj.property);
} catch (e) {
  console.error("Cannot read property of undefined");
}
```

### 6. ❌ Type Error

Генерує помилку типу:

```javascript
try {
  const num = null;
  num.toFixed(2);
} catch (e) {
  console.error("TypeError: Cannot read property toFixed");
}
```

### 7. ⚠️ Network Error

Генерує помилку мережі:

```javascript
fetch("https://invalid-url-that-does-not-exist.com").catch((error) =>
  console.error("Network Error:", error.message)
);
```

### 8. ℹ️ Multiple Errors

Генерує кілька помилок одночасно:

```javascript
console.error("Помилка 1");
console.warn("Попередження 1");
console.error("Помилка 2");
console.warn("Попередження 2");
```

## 🚀 Як використовувати:

### Крок 1: Відкрийте DevHelper

Натисніть кнопку 🐛 внизу справа екрану

### Крок 2: Перейдіть на вкладку "🧪 Тестування"

Виберіть вкладку в верхній частині модального вікна

### Крок 3: Натисніть на тест

Виберіть будь-який тип помилки для генерації

### Крок 4: Перегляньте результати

Перейдіть на вкладку "Помилки" щоб побачити згенеровані помилки

### Крок 5: Аналізуйте з AI (опціонально)

Натисніть "🤖 AI" для отримання детального аналізу помилки

## 💡 Приклад використання:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Testing DevHelper</title>
  </head>
  <body>
    <h1>My App</h1>

    <!-- DevHelper Script -->
    <script src="https://your-domain.com/api/devhelper/script"></script>
    <script>
      DevHelper.init({
        apiKey: "your-api-key",
        projectId: "test-project",
        devMode: true,
      });
    </script>
  </body>
</html>
```

**Тестування:**

1. Відкрийте сторінку в браузері
2. Натисніть 🐛 → "🧪 Тестування"
3. Натисніть "❌ Console Error"
4. Перейдіть на "Помилки" → побачите 1 помилку
5. Натисніть "🤖 AI" → отримаєте аналіз

## 🎨 UI вкладки:

```
🧪 Тестування

Натисніть на кнопки нижче, щоб згенерувати тестові помилки:

┌─────────────────┬─────────────────┐
│ ❌ Console Error│ ⚠️ Console Warn │
├─────────────────┼─────────────────┤
│ ⚡ Runtime Error│ ℹ️ Promise Rej. │
├─────────────────┼─────────────────┤
│ ❌ Undefined Err│ ❌ Type Error   │
├─────────────────┼─────────────────┤
│ ⚠️ Network Error│ ℹ️ Multiple Err │
└─────────────────┴─────────────────┘

💡 Підказка: Після виклику помилок, перейдіть на
вкладку "Помилки" та натисніть "🤖 AI" для
отримання детального аналізу.
```

## 🔧 Технічні деталі:

### Глобальні функції:

Кожна кнопка викликає глобальну функцію:

```javascript
window.testConsoleError = function () {
  console.error("Тестова помилка від DevHelper");
};

window.testConsoleWarning = function () {
  console.warn("Тестове попередження від DevHelper");
};

// ... інші функції
```

### Перехоплення помилок:

DevHelper автоматично перехоплює всі помилки:

```javascript
// Console errors
console.error = function (...args) {
  captureError("error", args);
  originalConsole.error.apply(console, args);
};

// Runtime errors
window.addEventListener("error", function (event) {
  captureError("error", [event.message], {
    stack: event.error?.stack,
    url: event.filename,
    lineNumber: event.lineno,
    columnNumber: event.colno,
  });
});

// Promise rejections
window.addEventListener("unhandledrejection", function (event) {
  captureError("error", [event.reason]);
});
```

## 📊 Що відбувається після тесту:

### 1. Помилка перехоплюється

DevHelper автоматично ловить помилку

### 2. Зберігається в масиві

```javascript
errors.push({
  type: "error",
  message: "Тестова помилка від DevHelper",
  timestamp: Date.now(),
  stack: "...",
  fileName: "test.html",
  lineNumber: 42,
  columnNumber: 10,
});
```

### 3. Оновлюється badge

Червоний badge на кнопці 🐛 показує кількість помилок

### 4. Доступна для аналізу

Можна переглянути на вкладці "Помилки" та проаналізувати з AI

## 🎓 Навчальні сценарії:

### Сценарій 1: Демонстрація команді

1. Відкрийте DevHelper
2. Покажіть вкладку "🧪 Тестування"
3. Згенеруйте кілька помилок
4. Покажіть як вони відображаються
5. Проаналізуйте з AI

### Сценарій 2: Тестування інтеграції

1. Підключіть DevHelper до проекту
2. Згенеруйте тестові помилки
3. Перевірте чи вони відправляються на сервер
4. Перевірте чи AI аналіз працює

### Сценарій 3: Навчання junior розробників

1. Покажіть різні типи помилок
2. Поясніть чим вони відрізняються
3. Покажіть як їх відловлювати
4. Покажіть як їх виправляти

## ⚠️ Важливо:

1. **Тільки для розробки**: Вкладка доступна тільки в `devMode: true`
2. **Не для production**: Не використовуйте на продакшн серверах
3. **Тестові дані**: Всі помилки позначені як "Тестова"
4. **Очищення**: Не забувайте очищати помилки після тестування

## 🔒 Безпека:

- Тестові функції доступні тільки коли відкрита модалка
- Не впливають на роботу додатку
- Не відправляються на сервер автоматично (якщо не налаштовано `autoReport`)
- Можна очистити в будь-який момент

## 💡 Корисні поради:

1. **Тестуйте перед релізом**: Переконайтесь що DevHelper працює
2. **Показуйте клієнтам**: Демонструйте можливості моніторингу
3. **Навчайте команду**: Використовуйте для онбордингу
4. **Дебажте проблеми**: Якщо щось не працює - згенеруйте тестову помилку

## 🎉 Приклад повного workflow:

```javascript
// 1. Підключення
DevHelper.init({
  apiKey: "sk_test_123",
  projectId: "my-app",
  devMode: true,
  autoReport: false, // Не відправляємо тестові помилки
});

// 2. Відкриваємо DevHelper
// Натискаємо 🐛

// 3. Тестуємо
// 🧪 Тестування → ❌ Console Error

// 4. Перевіряємо
// Помилки (1) → бачимо помилку

// 5. Аналізуємо
// 🤖 AI → отримуємо аналіз

// 6. Очищаємо
// Очистити → готово до нових тестів
```

Тепер у вас є зручний інструмент для тестування системи відловлювання помилок! 🚀
